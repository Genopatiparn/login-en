# 🔒 คู่มือระบบ Security Questions Password Reset

## 📋 ภาพรวม
ระบบรีเซ็ตรหัสผ่านด้วยคำถามความปลอดภัยที่สร้างจากข้อมูลผู้ใช้ ไม่ต้องพึ่งอีเมลหรือข้อมูลภายนอก

## 🎯 จุดเด่นของระบบนี้
- **ไม่ต้องเช็คอีเมล** - ใช้ username แทน
- **คำถามสร้างอัตโนมัติ** - จากข้อมูลที่มีอยู่
- **ปลอดภัย** - ต้องตอบถูก 2/2 ข้อ
- **ไม่ต้องตั้งค่าเพิ่ม** - ใช้ข้อมูลในระบบ

## 🔄 ขั้นตอนการทำงาน

### Step 1: ขอรีเซ็ตรหัสผ่าน
```
POST /api/users/forgotpassword
{
  "username": "testuser"
}
```

### Step 2: ได้รับคำถามความปลอดภัย
```json
{
  "message": "กรุณาตอบคำถามความปลอดภัย",
  "sessionToken": "abc123...",
  "userInfo": {
    "username": "testuser",
    "firstName": "J***",
    "lastName": "D***",
    "email": "j***@example.com"
  },
  "securityQuestions": [
    {
      "id": 1,
      "question": "อีเมลของคุณขึ้นต้นด้วยอักษรอะไร?",
      "hint": "ตัวอักษรแรกของอีเมล j***"
    },
    {
      "id": 2,
      "question": "นามสกุลของคุณมีกี่ตัวอักษร?",
      "hint": "นามสกุล: D***"
    }
  ]
}
```

### Step 3: ตอบคำถาม
```
POST /api/users/verify-security-answers
{
  "sessionToken": "abc123...",
  "answers": [
    {"id": 1, "answer": "j"},
    {"id": 2, "answer": "3"}
  ]
}
```

### Step 4: ได้รับ Reset Token
```json
{
  "message": "ตอบคำถามถูกต้อง! ได้รับ Reset Token",
  "success": true,
  "resetData": {
    "username": "testuser",
    "resetToken": "def456...",
    "expiryTime": "2024-12-24T11:30:00.000Z",
    "correctAnswers": 2
  }
}
```

### Step 5: รีเซ็ตรหัสผ่าน
```
POST /api/users/resetpassword
{
  "token": "def456...",
  "newPassword": "mynewpassword"
}
```

## 🎯 API Endpoints

### 1. 🔑 Forgot Password (สร้างคำถาม)
**Endpoint:** `POST /api/users/forgotpassword`

**Request:**
```json
{
  "username": "testuser"
}
```

**Response:**
- สร้างคำถามความปลอดภัย 3 ข้อ
- สุ่มเลือก 2 ข้อให้ตอบ
- สร้าง Session Token (หมดอายุ 10 นาที)
- แสดงข้อมูลผู้ใช้แบบซ่อน

### 2. 🔍 Verify Security Answers
**Endpoint:** `POST /api/users/verify-security-answers`

**Request:**
```json
{
  "sessionToken": "abc123...",
  "answers": [
    {"id": 1, "answer": "j"},
    {"id": 2, "answer": "3"}
  ]
}
```

**Response:**
- ตรวจสอบคำตอบ (ต้องถูก 2/2 ข้อ)
- สร้าง Reset Token (หมดอายุ 1 ชั่วโมง)
- ล้าง Session และข้อมูลชั่วคราว

### 3. 🔄 Reset Password
**Endpoint:** `POST /api/users/resetpassword`

เหมือนระบบเดิม - ใช้ Token รีเซ็ตรหัสผ่าน

### 4. 🎯 Test Security Questions (Dev Only)
**Endpoint:** `POST /api/users/test-security-questions`

**Request:**
```json
{
  "username": "testuser"
}
```

**Response:**
- แสดงคำถามและคำตอบทั้งหมด
- สำหรับการทดสอบเท่านั้น

## 🔒 คำถามความปลอดภัย

### คำถามที่ 1: อีเมลขึ้นต้นด้วยอักษรอะไร?
- **ตัวอย่าง:** john@example.com → คำตอบ: "j"
- **คำใบ้:** "ตัวอักษรแรกของอีเมล j***"

### คำถามที่ 2: นามสกุลมีกี่ตัวอักษร?
- **ตัวอย่าง:** "Doe" → คำตอบ: "3"
- **คำใบ้:** "นามสกุล: D***"

### คำถามที่ 3: ชื่อขึ้นต้นและลงท้ายด้วยอักษรอะไร?
- **ตัวอย่าง:** "John" → คำตอบ: "jn"
- **คำใบ้:** "ชื่อ: J***n"

## 🛡️ ความปลอดภัย

### การสร้างคำถาม:
- ✅ สุ่มเลือก 2 จาก 3 คำถาม
- ✅ แสดงข้อมูลแบบซ่อน (J***, D***, j***@example.com)
- ✅ Session Token หมดอายุ 10 นาที

### การตรวจสอบคำตอบ:
- ✅ ต้องตอบถูกทั้ง 2 ข้อ
- ✅ ไม่แยกแยะตัวพิมพ์เล็ก-ใหญ่
- ✅ ล้างข้อมูลชั่วคราวหลังใช้

### การรีเซ็ตรหัสผ่าน:
- ✅ Reset Token หมดอายุ 1 ชั่วโมง
- ✅ ใช้ได้ครั้งเดียว
- ✅ เข้ารหัสด้วย bcrypt

## 🧪 การทดสอบ

### ทดสอบด้วยสคริปต์:
```bash
node test-security-questions.js        # ทดสอบระบบทั้งหมด
node test-security-questions.js --wrong # ทดสอบคำตอบผิด
```

### ทดสอบด้วย Manual:

#### 1. ดูคำตอบสำหรับทดสอบ:
```bash
POST /api/users/test-security-questions
{
  "username": "testuser"
}
```

#### 2. เริ่มกระบวนการ:
```bash
POST /api/users/forgotpassword
{
  "username": "testuser"
}
```

#### 3. ตอบคำถาม:
```bash
POST /api/users/verify-security-answers
{
  "sessionToken": "...",
  "answers": [...]
}
```

## 🎯 ตัวอย่างการใช้งานจริง

### Scenario: ผู้ใช้ลืมรหัสผ่าน

**ผู้ใช้:** "ผมลืมรหัสผ่าน username: john123"

**ระบบ:** สร้างคำถาม
- อีเมลขึ้นต้นด้วยอักษรอะไร? (คำใบ้: j***)
- นามสกุลมีกี่ตัวอักษร? (คำใบ้: D***)

**ผู้ใช้:** ตอบ "j" และ "3"

**ระบบ:** ✅ ถูกต้อง! ได้ Reset Token

**ผู้ใช้:** ใช้ Token รีเซ็ตรหัสผ่านใหม่

## 🔄 เปรียบเทียบกับระบบอื่น

| ฟีเจอร์ | อีเมล | Security Questions | Admin Reset |
|---------|-------|-------------------|-------------|
| ต้องตั้งค่า | ✅ ซับซ้อน | ❌ ไม่ต้อง | ❌ ไม่ต้อง |
| ความเร็ว | ช้า | เร็ว | เร็ว |
| ผู้ใช้ทำเอง | ✅ ได้ | ✅ ได้ | ❌ ต้องแอดมิน |
| ความปลอดภัย | สูง | ปานกลาง | สูง |
| การทดสอบ | ยาก | ง่าย | ง่าย |

## ⚠️ ข้อควรระวัง

1. **API test-security-questions** ใช้สำหรับ Development เท่านั้น
2. **ข้อมูลผู้ใช้** ต้องมีชื่อ นามสกุล อีเมล ครบถ้วน
3. **Session Token** หมดอายุเร็ว (10 นาที)
4. **คำตอบ** ต้องถูกทั้ง 2 ข้อ

## 🚀 ข้อดี

- **ไม่ต้องพึ่งอีเมล** - ใช้ข้อมูลที่มีอยู่
- **ผู้ใช้ทำเอง** - ไม่ต้องรอแอดมิน
- **ปลอดภัยพอสมควร** - ต้องรู้ข้อมูลส่วนตัว
- **ง่ายต่อการทดสอบ** - มี API สำหรับ Dev
- **ไม่ต้องตั้งค่าเพิ่ม** - ใช้งานได้ทันที

ระบบนี้เหมาะสำหรับองค์กรที่ต้องการให้ผู้ใช้สามารถรีเซ็ตรหัสผ่านเองได้ โดยไม่ต้องพึ่งระบบอีเมลหรือแอดมิน!