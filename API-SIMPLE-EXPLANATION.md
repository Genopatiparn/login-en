# 📚 คู่มือการทำงานของ API แบบเข้าใจง่าย

## 📋 สารบัญ
1. [🔐 Authentication APIs - ระบบเข้าสู่ระบบ](#authentication-apis)
2. [👥 Person Management APIs - จัดการข้อมูลบุคคล](#person-management-apis)
3. [🔧 Middleware Functions - ฟังก์ชันช่วย](#middleware-functions)

---

## 🧹 การอัพเดทล่าสุด (Cleanup)
- ✅ ลบระบบ Admin Reset ที่ไม่จำเป็นออกแล้ว
- ✅ ลบไฟล์ adminReset.js และ routes ที่เกี่ยวข้อง
- ✅ ทำความสะอาดโค้ดที่ซ้ำซ้อน
- ✅ ระบบใช้การยืนยันตัวตนแบบง่าย (username + email + phone) สำหรับรีเซ็ตรหัสผ่าน

---

# 🔐 Authentication APIs

## 1. 📝 register.js - สมัครสมาชิก

### 🎯 ทำอะไร
สร้างบัญชีผู้ใช้ใหม่ในระบบ

### 📥 ข้อมูลที่ต้องส่งมา
```javascript
{
  "username": "ชื่อผู้ใช้",
  "password": "รหัสผ่าน", 
  "email": "อีเมล",
  "firstName": "ชื่อจริง",
  "lastName": "นามสกุล"
}
```

### 🔄 ขั้นตอนการทำงาน

#### 1. รับข้อมูลจากผู้ใช้
```javascript
const { username, password, email, firstName, lastName } = req.body;
```
**อธิบาย:** แยกข้อมูลที่ส่งมาออกเป็นตัวแปรแยกๆ

#### 2. ตรวจสอบข้อมูลครบถ้วน
```javascript
if (!username) {
  return res.status(400).json({ error: 'กรุณาระบุชื่อผู้ใช้งาน' });
}
```
**อธิบาย:** ตรวจสอบว่าข้อมูลสำคัญครบหรือไม่ ถ้าไม่ครบจะส่ง error กลับไป

#### 3. ตรวจสอบข้อมูลซ้ำ
```javascript
const existUser = await User.findOne({ username: username });
if (existUser) {
  return res.status(400).json({ error: 'ชื่อผู้ใช้งานนี้ถูกใช้งานแล้ว' });
}
```
**อธิบาย:** 
- `User.findOne()` = ค้นหาผู้ใช้ในฐานข้อมูล
- ถ้าเจอแล้ว = ชื่อผู้ใช้ซ้ำ ไม่ให้สมัคร

#### 4. เข้ารหัสรหัสผ่าน
```javascript
const saltRounds = 10;
const hashedPassword = await bcrypt.hash(password, saltRounds);
```
**อธิบาย:**
- `bcrypt.hash()` = เข้ารหัสรหัสผ่านให้ปลอดภัย
- `saltRounds = 10` = ความแข็งแกร่งของการเข้ารหัส
- รหัสผ่านจะกลายเป็นตัวอักษรยาวๆ ที่ไม่สามารถถอดรหัสกลับได้

#### 5. สร้างผู้ใช้ใหม่
```javascript
const newUser = new User({
  username: username,
  password: hashedPassword,
  email: email,
  firstName: firstName,
  lastName: lastName
});
```
**อธิบาย:** สร้าง object ผู้ใช้ใหม่พร้อมข้อมูลที่ได้รับ

#### 6. บันทึกลงฐานข้อมูล
```javascript
const savedUser = await newUser.save();
```
**อธิบาย:** 
- `save()` = บันทึกข้อมูลลงฐานข้อมูล
- `await` = รอให้บันทึกเสร็จก่อน

#### 7. ส่งผลลัพธ์กลับ
```javascript
res.status(201).json({
  message: 'ลงทะเบียนสมาชิกเรียบร้อยแล้ว',
  user: {
    username: savedUser.username,
    email: savedUser.email
  }
});
```
**อธิบาย:** ส่งข้อความยืนยันและข้อมูลผู้ใช้กลับไป (ไม่ส่งรหัสผ่าน)

---

## 2. 🔑 login.js - เข้าสู่ระบบ

### 🎯 ทำอะไร
ตรวจสอบชื่อผู้ใช้และรหัสผ่าน แล้วให้เข้าสู่ระบบ

### 📥 ข้อมูลที่ต้องส่งมา
```javascript
{
  "username": "ชื่อผู้ใช้ หรือ อีเมล",
  "password": "รหัสผ่าน"
}
```

### 🔄 ขั้นตอนการทำงาน

#### 1. รับข้อมูลและเตรียม
```javascript
const { username, email, password } = req.body;
const usernameOrEmail = username || email;
```
**อธิบาย:** 
- รับข้อมูลที่ส่งมา
- `||` = ถ้ามี username ใช้ username ถ้าไม่มีใช้ email

#### 2. ค้นหาผู้ใช้
```javascript
const user = await User.findOne({
  $or: [
    { username: usernameOrEmail },
    { email: usernameOrEmail }
  ]
});
```
**อธิบาย:**
- `$or` = ค้นหาแบบ "หรือ" 
- หาจาก username หรือ email ก็ได้
- ผู้ใช้สามารถใส่ username หรือ email เพื่อ login

#### 3. ตรวจสอบรหัสผ่าน
```javascript
const isPasswordValid = await bcrypt.compare(password, user.password);
```
**อธิบาย:**
- `bcrypt.compare()` = เปรียบเทียบรหัสผ่าน
- เอารหัสผ่านที่ป้อนมาเปรียบเทียบกับรหัสที่เข้ารหัสในฐานข้อมูล
- ได้ผลลัพธ์ true (ถูก) หรือ false (ผิด)

#### 4. จัดการสถานะ Login
```javascript
const existingLogin = await LoggedInUser.findOne({ username: user.username });
if (existingLogin) {
  existingLogin.loginTime = new Date();
  await existingLogin.save();
} else {
  const loggedInUser = new LoggedInUser({ username: user.username });
  await loggedInUser.save();
}
```
**อธิบาย:**
- ตรวจสอบว่าผู้ใช้ login อยู่แล้วหรือไม่
- ถ้า login อยู่แล้ว = อัปเดตเวลา login ใหม่
- ถ้ายัง = สร้างสถานะ login ใหม่

#### 5. ส่งผลลัพธ์สำเร็จ
```javascript
res.json({
  message: 'เข้าสู่ระบบสำเร็จ',
  user: {
    username: user.username,
    email: user.email,
    role: user.role
  }
});
```

---

## 3. 🔄 changePassword.js - เปลี่ยนรหัสผ่าน

### 🎯 ทำอะไร
เปลี่ยนรหัสผ่านของผู้ใช้

### 📥 ข้อมูลที่ต้องส่งมา
```javascript
{
  "username": "ชื่อผู้ใช้",
  "oldPassword": "รหัสผ่านเก่า",
  "newPassword": "รหัสผ่านใหม่"
}
```

### 🔄 ขั้นตอนการทำงาน

#### 1. ค้นหาผู้ใช้
```javascript
const user = await User.findOne({ username: username });
```

#### 2. ตรวจสอบรหัสผ่านเก่า
```javascript
const isOldPasswordValid = await bcrypt.compare(oldPassword, user.password);
```
**อธิบาย:** ต้องใส่รหัสเก่าถูกต้องก่อนถึงจะเปลี่ยนได้

#### 3. เข้ารหัสรหัสผ่านใหม่
```javascript
const hashedNewPassword = await bcrypt.hash(newPassword, 10);
```

#### 4. อัปเดตในฐานข้อมูล
```javascript
await User.findOneAndUpdate(
  { username: username },
  { password: hashedNewPassword }
);
```
**อธิบาย:** 
- `findOneAndUpdate()` = ค้นหาและอัปเดตในคำสั่งเดียว
- อัปเดตรหัสผ่านเป็นรหัสใหม่ที่เข้ารหัสแล้ว

---

## 4. 🚪 logout.js - ออกจากระบบ

### 🎯 ทำอะไร
ลบสถานะการเข้าสู่ระบบ

### 🔄 ขั้นตอนการทำงาน

#### 1. ค้นหาผู้ใช้
```javascript
const user = await User.findOne({
  $or: [
    { username: usernameOrEmail },
    { email: usernameOrEmail }
  ]
});
```

#### 2. ลบสถานะ Login
```javascript
await LoggedInUser.deleteOne({ username: user.username });
```
**อธิบาย:**
- `deleteOne()` = ลบข้อมูลออกจากฐานข้อมูล
- ลบสถานะการ login ออก = ออกจากระบบ

---

## 5. 🔑 forgotPassword.js - ลืมรหัสผ่าน (ระบบยืนยันตัวตนแบบง่าย)

### 🎯 ทำอะไร
รีเซ็ตรหัสผ่านด้วยการยืนยันตัวตน (username + email + phone) - ง่าย รวดเร็ว ปลอดภัย

### 📥 ข้อมูลที่ต้องส่งมา
```javascript
{
  "username": "testuser",           // ชื่อผู้ใช้งาน
  "email": "test@example.com",      // อีเมลเพื่อยืนยันตัวตน
  "phone": "0812345678",           // เบอร์โทรเพื่อยืนยันตัวตน
  "newPassword": "newpass123"      // รหัสผ่านใหม่ที่ต้องการตั้ง
}
```

### 🔄 ขั้นตอนการทำงาน

#### 1. ตรวจสอบข้อมูลครบถ้วน
```javascript
if (!username || !email || !phone || !newPassword) {
  return res.status(400).json({ error: 'กรุณาระบุข้อมูลให้ครบถ้วน' });
}
```
**อธิบาย:** ตรวจสอบว่าข้อมูลทั้ง 4 อย่างครบหรือไม่

#### 2. หาผู้ใช้ในระบบ
```javascript
const user = await User.findOne({ username: username });
if (!user) {
  return res.status(404).json({ error: 'ไม่พบชื่อผู้ใช้งานนี้ในระบบ' });
}
```
**อธิบาย:** หาผู้ใช้ด้วย username

#### 3. ยืนยันตัวตน
```javascript
if (user.email !== email || user.phone !== phone) {
  return res.status(401).json({ 
    error: 'ข้อมูลยืนยันตัวตน (อีเมลหรือเบอร์โทรศัพท์) ไม่ถูกต้อง' 
  });
}
```
**อธิบาย:** ตรวจสอบว่าอีเมลและเบอร์โทรตรงกับที่มีในฐานข้อมูลหรือไม่

#### 4. เข้ารหัสรหัสผ่านใหม่
```javascript
const saltRounds = 10;
const hashedNewPassword = await bcrypt.hash(newPassword, saltRounds);
```
**อธิบาย:** เข้ารหัสรหัสผ่านใหม่ด้วย bcrypt เพื่อความปลอดภัย

#### 5. อัปเดตรหัสผ่าน
```javascript
await User.findOneAndUpdate(
  { username: username },
  { password: hashedNewPassword }
);
```
**อธิบาย:** บันทึกรหัสผ่านใหม่ที่เข้ารหัสแล้วลงฐานข้อมูล

### 🛡️ ความปลอดภัย
- **ยืนยันตัวตนหลายชั้น** - ต้องรู้ username + email + phone
- **เข้ารหัสรหัสผ่าน** - ใช้ bcrypt saltRounds = 10
- **ไม่มี token หรือ session** - ลดความซับซ้อน
- **เปลี่ยนทันที** - ไม่ต้องรอขั้นตอนเพิ่มเติม

### 🎯 ข้อดี
- **ง่ายมาก** - 1 ขั้นตอนเดียว
- **ไม่ต้องตั้งค่าอีเมล** - ใช้งานได้ทันที
- **ผู้ใช้ทำเอง** - ไม่ต้องรอแอดมิน
- **ปลอดภัยพอสมควร** - ต้องรู้ข้อมูลส่วนตัว 3 อย่าง
- **รวดเร็ว** - เปลี่ยนรหัสผ่านได้ทันที

---

# 👥 Person Management APIs

## 6. 📋 getAllPersons.js - ดูข้อมูลบุคคลทั้งหมด

### 🎯 ทำอะไร
ดึงข้อมูลบุคคลทั้งหมดจากฐานข้อมูล

### 🔄 ขั้นตอนการทำงาน

```javascript
Person.find({})
  .then(function(persons) {
    res.json({
      message: 'ดึงข้อมูลบุคคลทั้งหมดสำเร็จ',
      persons: persons,
      total: persons.length
    });
  })
```
**อธิบาย:**
- `Person.find({})` = ดึงข้อมูลทั้งหมด (ไม่มีเงื่อนไข)
- `.then()` = เมื่อดึงข้อมูลเสร็จแล้วทำอะไร
- ส่งข้อมูลและจำนวนทั้งหมดกลับไป

---

## 7. 🔍 getPersonById.js - ดูข้อมูลบุคคลตาม ID

### 🎯 ทำอะไร
ดึงข้อมูลบุคคลคนเดียวตาม ID

### 🔄 ขั้นตอนการทำงาน

#### 1. รับ ID จาก URL
```javascript
const id = req.params.id;
```
**อธิบาย:** ดึง ID จาก URL เช่น `/api/persons/123` จะได้ id = "123"

#### 2. ค้นหาบุคคล
```javascript
const person = await Person.findOne({ id: id });
if (!person) {
  return res.status(404).json({ error: 'ไม่พบข้อมูลบุคคล' });
}
```
**อธิบาย:** ค้นหาด้วย ID ถ้าไม่เจอส่ง error 404

---

## 8. ➕ createPerson.js - เพิ่มข้อมูลบุคคล

### 🎯 ทำอะไร
สร้างข้อมูลบุคคลใหม่ (บังคับใส่รูปภาพ)

### 🔄 ขั้นตอนการทำงาน

#### 1. ตรวจสอบรูปภาพ
```javascript
if (!req.uploadedImage && !req.body.image) {
  return res.status(400).json({ 
    error: 'กรุณาอัปโหลดรูปภาพ (ขนาดไม่เกิน 70KB)' 
  });
}
```
**อธิบาย:** บังคับต้องมีรูปภาพ

#### 2. เตรียมข้อมูล
```javascript
const personData = {
  firstName: req.body.firstName,
  lastName: req.body.lastName,
  email: req.body.email,
  image: imagePath,
  // ... ข้อมูลอื่นๆ อีกเยอะ
};
```

#### 3. บันทึกลงฐานข้อมูล
```javascript
const person = new Person(personData);
person.save()
  .then(function(newPerson) {
    res.status(201).json({
      message: 'สร้างข้อมูลเรียบร้อยแล้ว',
      person: newPerson
    });
  })
```

---

## 9. ✏️ updatePerson.js - แก้ไขข้อมูลบุคคล

### 🎯 ทำอะไร
อัปเดตข้อมูลบุคคลที่มีอยู่

### 🔄 ขั้นตอนการทำงาน

#### 1. รับ ID และข้อมูลใหม่
```javascript
const id = req.params.id;
const updateData = { ...req.body };
```

#### 2. อัปเดตฐานข้อมูล
```javascript
const updatedPerson = await Person.findOneAndUpdate(
  { id: id }, 
  updateData, 
  { new: true }
);
```
**อธิบาย:**
- หาบุคคลด้วย ID
- อัปเดตด้วยข้อมูลใหม่
- `{ new: true }` = ส่งข้อมูลหลังอัปเดตกลับมา

---

## 10. 🗑️ deletePerson.js - ลบข้อมูลบุคคล

### 🎯 ทำอะไร
ลบข้อมูลบุคคลออกจากระบบ

### 🔄 ขั้นตอนการทำงาน

#### 1. ค้นหาและลบ
```javascript
const person = await Person.findOne({ id: id });
if (person) {
  await Person.findOneAndDelete({ id: id });
}
```
**อธิบาย:**
- `Person.findOne({ id: id })` = ค้นหาบุคคลด้วย ID ก่อน
- ถ้าเจอ (`if (person)`) = มีข้อมูลจริง
- `Person.findOneAndDelete({ id: id })` = ค้นหาและลบในคำสั่งเดียว
- `await` = รอให้การลบเสร็จก่อนไปต่อ

**ทำไมต้องค้นหาก่อน?**
- เพื่อตรวจสอบว่ามีข้อมูลจริงหรือไม่
- ถ้าไม่มีจะได้ส่ง error message ที่เหมาะสม

---

# 🔧 Middleware Functions

## 11. 📤 handleFileUpload - จัดการอัปโหลดไฟล์

### 🎯 ทำอะไร
จัดการการอัปโหลดรูปภาพอย่างปลอดภัย

### 🔄 การทำงาน

#### 1. ตั้งค่าการเก็บไฟล์
```javascript
const storage = multer.diskStorage({
  destination: 'uploads/',
  filename: function (req, file, cb) {
    const uniqueName = 'image-' + Date.now() + '-' + Math.random();
    cb(null, uniqueName);
  }
});
```
**อธิบาย:**
- เก็บไฟล์ในโฟลเดอร์ `uploads/`
- สร้างชื่อไฟล์ไม่ซ้ำด้วย timestamp + random number

#### 2. ตั้งค่าข้อจำกัด
```javascript
const upload = multer({ 
  storage: storage,
  limits: {
    fileSize: 70 * 1024 // 70KB
  },
  fileFilter: function (req, file, cb) {
    const allowedTypes = /jpeg|jpg|png|gif/;
    if (allowedTypes.test(file.mimetype)) {
      cb(null, true);
    } else {
      cb(new Error('รองรับเฉพาะไฟล์รูปภาพ'));
    }
  }
});
```
**อธิบาย:**
- จำกัดขนาดไฟล์ไม่เกิน 70KB
- รองรับเฉพาะไฟล์รูปภาพ (jpg, png, gif)

---

## 12. ✅ validatePersonData - ตรวจสอบข้อมูล

### 🎯 ทำอะไร
ตรวจสอบความถูกต้องของข้อมูลบุคคล

### 🔄 การทำงาน

```javascript
if (!req.body.firstName) {
  return res.status(400).json({ error: 'กรุณาระบุชื่อ' });
}
if (!req.body.lastName) {
  return res.status(400).json({ error: 'กรุณาระบุนามสกุล' });
}
```
**อธิบาย:** ตรวจสอบข้อมูลบังคับว่าครบหรือไม่

---

## 13. 🔗 checkDatabaseConnection - ตรวจสอบฐานข้อมูล

### 🎯 ทำอะไร
ตรวจสอบการเชื่อมต่อฐานข้อมูลก่อนทำงาน

### 🔄 การทำงาน

```javascript
if (mongoose.connection.readyState !== 1) {
  return res.status(500).json({ error: 'ไม่ได้เชื่อมต่อฐานข้อมูล' });
}
next(); // ส่งต่อไปขั้นตอนถัดไป
```
**อธิบาย:**
- `readyState = 1` หมายถึงเชื่อมต่อแล้ว
- ถ้าไม่ได้เชื่อมต่อจะหยุดทำงาน

---

# 📊 สรุป

## 🔄 ลำดับการทำงานทั่วไป
1. **รับ Request** → ได้ข้อมูลจากผู้ใช้
2. **ตรวจสอบข้อมูล** → ดูว่าข้อมูลถูกต้องหรือไม่
3. **ทำงานกับฐานข้อมูล** → บันทึก/ดึง/อัปเดต/ลบ
4. **ส่ง Response** → ส่งผลลัพธ์กลับไป

## 🛡️ ความปลอดภัย
- **เข้ารหัสรหัสผ่าน** ด้วย bcrypt
- **ตรวจสอบไฟล์** ขนาดและประเภท
- **ตรวจสอบข้อมูล** ก่อนบันทึก
- **ไม่เปิดเผยข้อมูลสำคัญ** ในข้อความ error

## 🎯 จุดสำคัญ
- **async/await** = รอให้ทำงานเสร็จก่อน
- **findOne()** = หาข้อมูลหนึ่งรายการ
- **find()** = หาข้อมูลทั้งหมด
- **save()** = บันทึกข้อมูล
- **deleteOne()** = ลบข้อมูล
- **bcrypt** = เข้ารหัสรหัสผ่าน

---

**เอกสารนี้อธิบายการทำงานของ API ทั้งหมดในระบบอย่างเข้าใจง่าย**